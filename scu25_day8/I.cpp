#include <bits/stdc++.h>
using namespace std;

void solve() {
    long long n, x;
    cin >> n;

    cout << "mul 9" << endl;
    cout.flush();
    cin >> x;
    
    cout << "digit" << endl;
    cout.flush();
    cin >> x;
    
    cout << "digit" << endl;
    cout.flush();
    cin >> x;
    
    cout << "add " << n - 9 << endl;
    cout.flush();
    cin >> x;
    
    cout << "!" << endl;
    cout.flush();
    cin >> x;
}
int main() {
    ios::sync_with_stdio(false);
    cin.tie(NULL);
    cout.tie(NULL);
    int t;
    cin >> t;
    while(t--) solve();

    return 0;
}