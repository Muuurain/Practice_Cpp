// template_value_to_index

vector<int> num(n);
vector<int> uniq(n);
for (int i = 0; i < n; ++i) {
    cin >> uniq[i];
}
num = uniq;

sort(uniq.begin(), uniq.end());
uniq.erase(unique(uniq.begin(), uniq.end()), uniq.end());
uniq.shrink_to_fit();

for(auto& x : num) {
    x = lower_bound(uniq.begin(), uniq.end(), x) - uniq.begin();
}