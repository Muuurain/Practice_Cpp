#include <bits/stdc++.h>
using namespace std;

typedef long long ll;
string s, ret;
string 
int main() {
    ios::sync_with_stdio(false);
    cin.tie(nullptr);
    cout.tie(nullptr);

    cin >> s;
    ll t = 1;
    cal(s);

    return 0;
}