vector<int> num(n);
vector<int> uniq;
for(int i = 0; i < n; i++) {
    cin >> num[i];
    uniq.push_back(num[i]);
}
sort(uniq.begin(), uniq.end());
uniq.erase(unique(uniq.begin(), uniq.end()), uniq.end());
for(int i = 0; i < n; i++) {
    num[i] = lower_bound(uniq.begin(), uniq.end(), num[i]) - uniq.begin();
}